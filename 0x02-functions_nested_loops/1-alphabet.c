#include "main.h"

/**
 * print_alphabet - This prints the alphabet in lower case
 * Return: Always 0.
 */
void print_alphabet(void)
{
	char lca;
	for (lca = 'a'; lca <= 'z'; lca++)
		_putchar(lca);

	_putchar('\n');
}
